Este projeto foi feito com React JS, para instala-lo corretamente basta abrir um terminal e digitar:

npm install

ou ainda:

yarn install

Após isso basta iniciar o projeto com o comando:

npm start

ou:

yarn start